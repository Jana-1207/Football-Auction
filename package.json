{
  "name": "football-auction",
  "private": true,
  "version": "1.0.0",
  "description": "Real-time football auction admin and display app powered by Supabase.",
  "scripts": {
    "generate-config": "node scripts/generate-config.js",
    "prepare": "node scripts/generate-config.js"
  }
}
